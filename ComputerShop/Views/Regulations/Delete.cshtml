@model ComputerShop.Models.Regulation

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Regulation</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Rule)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Rule)
        </dd>

    </dl>

@using (@Ajax.BeginForm("Delete", "Regulations", new AjaxOptions { HttpMethod = "POST", InsertionMode = InsertionMode.Replace, UpdateTargetId = "main" }))
{
    @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            <div>
                @Ajax.ActionLink("Powrót", "GetRegulations", "Admin", null, new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "main", InsertionMode = InsertionMode.Replace }, new { @class = "text" })
            </div>
        </div>
    }
</div>
