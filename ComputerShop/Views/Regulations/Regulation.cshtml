@model IEnumerable<ComputerShop.Models.Regulation>

@{
    ViewBag.Title = "Regulation";
}

<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">
            Regulamin
        </h1>
    </div>
</div>

<div class="col-lg-12">
    @foreach (var item in Model)
    {
        <div class="row">
            @Html.DisplayFor(x => item.Rule).ToString()
        </div>
    }
</div>
